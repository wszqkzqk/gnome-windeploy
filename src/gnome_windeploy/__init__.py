"""gnome-windeploy: deploy GNOME-platform desktop apps on Windows.

The GNOME equivalent of Qt's windeployqt: given compiled .exe files, produce a
redistributable, relocatable directory with the full transitive DLL closure and
all required GNOME runtime data.
"""

__version__ = "0.1.0"
